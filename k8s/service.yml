apiVersion: v1
kind: Service
metadata:
  name: fraud-detector-service
  labels:
    app: fraud-detector
spec:
  type: NodePort
  selector:
    app: fraud-detector
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080